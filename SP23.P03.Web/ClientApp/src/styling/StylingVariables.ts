const DEFAULT_SPACING = '0.5rem';

/**
 * This contains all the styling variables used in the app.
 */
export const STYLING_VARIABLES = {
    // ************ //
    // ** Sizing ** //
    // ************ //
    // App
    maxContentWidth: '2000px',
    defaultSpacing: DEFAULT_SPACING,
    defaultTitleFontSize: 'clamp(1.5rem, 2vw, 2.5rem)',
    defaultBodyFontSize: 'clamp(1rem, 1.5vw, 1.5rem)',

    // Header
    headerHeight: '60px',

    // rootContentStyles
    defaultRootContentPadding: `calc(2 * ${DEFAULT_SPACING}) ${DEFAULT_SPACING} ${DEFAULT_SPACING} ${DEFAULT_SPACING}`,
};
